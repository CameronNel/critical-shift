# Turbine Room

**Incomplete original build checkpoint.** The architectural design and a maintenance style slice exist. The complete turbine/generator hall is not yet modeled, the expansion gate has failed, and no final ten-camera set is accepted.

Read `CONTINUE.md` for exact state, defects, source paths and resume commands. `architecture/` contains the metric plan; `blender/` contains original reproducible source and the saved slice; `production/` contains independent review and technical evidence. Existing neighboring sections are untouched. Global module interfaces remain unbound.
