# Spawn R03 roof-light diagnosis — read-only CPU

Exact defective data found: linked original lights EEVEE_floor_bounce_BRIEFING (25 W), EEVEE_floor_bounce_HALL (40 W), EEVEE_floor_bounce_LOCKER (58 W) and EEVEE_floor_bounce_SERVICE (15 W) all have light.data.use_shadow=False. All four sit at Z=0.25 m. Their receiver and blocker linking collections are None. They therefore provide unoccluded illumination despite opaque roofing.

This is not missing membrane geometry: evaluated upward object raycasts from each fill light hit its corresponding opaque membrane at Z=3.56 m (briefing/hall/service) or 3.96 m (locker). Membranes have visible_shadow/camera/diffuse/glossy/transmission=True and hide_render=False. Their material is a simple Principled output with Alpha 1, Transmission Weight 0 and Emission Strength 0; no transparent shader is present. Adding further opaque caps cannot fix lights explicitly configured to ignore blockers. Original photometric practical lights inspected have shadows enabled. use_shadow_jitter is false but is not the shadow-disable setting; it does not explain missing occlusion.

Minimal assembly-only correction: use a local collection/object/light-data override for precisely these four linked fills and set use_shadow=True. Preserve their energy, colour, transforms and the source mesh data. Save only the generated assembly/exterior copy, not accepted.blend, module.blend or the original room file. Directly mutating/saving a source library is not required. If preserving the original interior fill effect exactly is essential, receiver linking restricted to original interior geometry is an alternative, but broader to implement and verify than the four shadow overrides.

This identifies a concrete light-occlusion bypass and verifies blocking surfaces exist. No diagnostic render was performed, so attribution of every white pixel and confirmation of elimination still requires the builder's same-camera rerender after the override. No additional speculative geometry repair is recommended. Roof diagnosis script/JSON: roof-diagnosis-R03.py/json. No model edits, saves or GPU use occurred.
