# Build authority

User requested a new Cooling Plant section in this isolated worktree using Astra Ultra and an independent Astra reviewer. The task owns only `sections/cooling-plant`, preserves reactor/turbine and completed areas, and requires original Blender construction, dimensioned interfaces, ten fixed-camera views, independent >=90 category scores, correction cycles and cold-start evidence before production completion.

The orchestrator briefs in the original checkout's `ops/facility-run/` control the build. The shared GPU gate and unique original-checkout status file are mandatory. On 8 September the run coordinator relayed a user wind-down instruction because account usage was low; the current task therefore stops after its queued S03 batch and preserves an honest incomplete checkpoint for later resumption.
