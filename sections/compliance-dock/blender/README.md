# Compliance Dock authoring checkpoint

Authoritative editable slice: `style_slice.blend`. Reproducible source: `build_dock.py`, factory empty, deterministic seed8217, Blender5.2. All geometry/materials are original authored code; no external geometry imports, paid assets, downloaded textures or copied approved-reference pixels.

Use `run.ps1` from the repository root. It sets private section resources and always routes render requests through the shared facility GPU gate. Existing revision folders are evidence; choose a new revision when correcting source. Examples and complete resume instructions are in section `CONTINUE.md`.

Full-stage dispatch is reserved for a later `full_dock.py` module, which has **not** been authored. The style slice remains below acceptance. The final environment and ten-camera cold-start package do not exist yet.

Support-dependent props use named assembly roots and actual support anchors. `validate_dock.py` evaluates saved geometry and measures those contacts. See `production/technical/VALIDATION.md` for metadata, invocation and limits.
