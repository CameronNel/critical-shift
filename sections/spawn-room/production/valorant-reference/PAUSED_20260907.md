# Saved pause checkpoint — 7 September 2026

The user asked to stop scene work, save it and sync it with GitHub. Do not resume scene edits without a later request.

## Authoritative saved files

- `../../blender/spawnroom_valorant_walk.blend` — pass-12 geometry plus the material changes described below. SHA256: `65c8a873da943e1936cd72d2a7b847938afc056adb9cfee87f5edf407b76d2b8`.
- `../../blender/valorant_room_palette.blend` — nine reusable materials, saved through Blender MCP. SHA256: `8c1fbc02f3b4c323f0d34cd549b225c37bbbdbbc58bc93e69d1afe6f91147ca4`.
- `room_palette_mcp_patches.json` — the nine validated/applied node patches, with original graph revisions. Transactional material backups remain inside the canonical file.
- `pass-12/source.blend` — unchanged last-rendered source, SHA256 `09713430c4841f9afa5da6a5ea963c0f525d3602ef8b4c4ed5779c2cef298eaf`.

Both active files were saved successfully with `bpy.ops.wm.save_as_mainfile` / `bpy.data.libraries.write` through MCP. The MCP claim was released. Pass 12 was visibly running in the native desktop Blender window before the final material edits. No stability claim is made for an indefinitely running session.

## Material stage saved after pass 12

Seven material copies isolate the three rooms: `V13_BRIEFING_wall`, `V13_BRIEFING_dado`, `V13_HALL_wall`, `V13_HALL_dado`, `V13_LOCKER_wall`, `V13_LOCKER_dado`, and `V13_LOCKER_V_ceiling_mineral`. Their existing `Reference palette balance` input was changed by validated structured MCP patches. Mesh slots were assigned by room prefix. The original source materials remain preserved.

Shared wall faces were assigned separately: positive-X faces on `HALL_right_ops`, `HALL_right_spawn`, `HALL_locker_infill`, and `HALL_room_header.001` use the locker wall material; negative-X faces on `HALL_left_ops`, `HALL_left_door_approach`, and `HALL_room_header` use the briefing wall material.

`V_pod_satin_metal` now has a 0.36–0.50 roughness range and 0.78 metallic value. `pod_optical_glass` keeps its blended transparent construction, with 0.16 principled-glass mix and RGB (0.82, 0.93, 0.98). All changed sockets were read back through MCP. These changes have **not** been rendered or independently scored yet.

No new lights, camera changes, pod scaling, paper layering, towel deformation, leaf additions or wear geometry were applied after pass 12. Such ideas discussed in the task remain proposed work.

## Resume point

Open the current canonical file and inspect the material stage before further changes. Do not run the full builder over it: `valorant_polish.py` currently reproduces pass 12, not the subsequent MCP palette. Either integrate the saved material library into reproduction or continue from the saved scene. Preserve the pass-12 source for comparison.

Latest completed independent review: `reviews/review-12.json` and `.md`. Lighting/art/layout: Briefing 82/86/89; Hall 82/87/85; Locker 79/85/84. The nine-score 90 gate is unmet. The reviewer identified local lighting balance, material-specific wear and pod composition as remaining issues; camera/projection differences should not prompt arbitrary architecture changes.

Pass 12 passed 17 inherited checks, 170 support contacts and 180 entrance/route rays. The nine direct user-constraint checks were last run on pass 07 and need repeating on the eventual final file. Avoid probe-cache GPU bakes: earlier attempts caused stalls and an observed AMD driver timeout; successful later renders used the retained architecture cache and local fill lights.

The original inhabited output remains unchanged: SHA256 `88d17ded84f51691628af1b9337f458dbe198834ee650a21b2f10858a0f3ac47`. The pre-existing local modification to `spawnroom_worn.blend` is outside this revision and is not staged. Its preserved local SHA256 is `d4f18160c2257fa346e32d5991993e47845b855f7e89b644f0e59bd8834e085a`.
