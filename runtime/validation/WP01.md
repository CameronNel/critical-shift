# WP-01 task and evidence record

**State: implementation in progress; native acceptance Blocked; independent review NotRun.**

Authorization: Cameron requested the reproducible Unity foundation on 17 September
2026. Primary author: ChatGPT. No independent reviewer has been assigned, and no
self-merge is permitted. Base: `a82c7d9b80456153a0c65ca4897044a7f62c7d1c`.
Task branch: `feat/wp01-unity-foundation-20260917`.

## Boundary and D-01 implementation profile

The requested WP-01 makes the previously proposed `runtime/unity/` root concrete.
Editor/version/revision are unchanged: Unity 6000.4.3f1 / 39d1a88d4dd1.
The scoped working profile is Built-in rendering, Mono, .NET Standard 2.1
(`NET_Standard`), managed stripping Disabled and normal domain/scene reload.
The compiler used for native builds is whatever the exact pinned editor supplies;
its compiler/API behaviour must be recorded by the native run, not inferred here.
Test Framework 1.4.6 and its declared ext.nunit 2.0.3 dependency are pinned using
Unity's public registry metadata. Native package resolution is still unverified.

These D-01 choices implement the requested bounded foundation. They are not
independent architecture approval, proof of native compatibility or closure of
D-01. The authoritative decision register remains
[DECISIONS_AND_RISKS.md](../../design/code-architecture/DECISIONS_AND_RISKS.md).
D-02 through D-09 are not decided by this task. No gameplay packages are installed.

The official release API reported that support for this selected editor ended
on 22 June 2026. The task preserves Cameron's recorded version rather than
silently upgrading. A later version change requires a scoped product decision.

## Prerequisite evidence already executed

Hosted preflight run `35255607023`, commit
`51307bd118ff142936b572c5ef1bcd23fc4f16a3`, checked only credential presence and
public editor/package metadata. No `UNITY_LICENSE`, `UNITY_EMAIL`, `UNITY_PASSWORD`
or `UNITY_SERIAL` secrets were configured. No values were logged or copied.
Remote Desktop Commander reported the linked workstation offline.
Preflight completion is not runtime acceptance.

## Acceptance and future evidence placement

Applicable contracts: A01/A02/A04/A06, H01/H04/H06/H07 and V00/V01.
The result-gating negative controls cover the narrow CI-01/02 failure modes;
full WP-02 graph, analyzer, negative-assembly and deletion tooling remain deferred.
Native expected identities are in `expected-tests.json`: 12 EditMode, 2 PlayMode.
Do not count the separate eight-test assertion harness as a Unity suite.

A valid native run must establish clean import, exact test discovery/results,
build success and a matching Player build identity with start/normal shutdown.
The runner preserves failure logs and never promotes missing evidence to Passed.
An unrun later phase remains NotRun after an earlier blocker.
The reviewed revision's executed results will be recorded in the PR and a compact
`execution-summary.json`; full logs stay in its identified workflow artifact.
No result is pre-filled as Passed by this task note.

## Expected changed paths and preservation

Only `runtime/`, the foundation workflow, the README navigation entry and the
D-01 progress note in the existing decision register may remain in the final diff.
One-off transfer/preflight workflows are removed after their completed runs.
No modification/deletion of `sections/`, `MAP.json`, `MAP.md`, existing art or
existing authoring workflows is allowed. `main` is not written or merged.

No existing runtime is replaced. No serialization migration or deletion of
production content is needed. Native preparation generates only the foundation
fixture, never a large hand-patched existing scene. Source metadata travels with
all first-party assets. Process lifetime belongs only to each bootstrap instance;
subscriptions are removed on destruction, and stop is idempotent.

## Definition of handoff

Deliver source and commands plus actual evidence, a bounded diff and a draft PR
when blocked. List native/external tests separately. Missing activation, native
scene/settings generation or independent review keeps WP-01 unaccepted. Completing
WP-01 alone does not pass Gate 0, which also requires WP-02. Do not begin gameplay
or map integration to conceal an incomplete foundation gate.
