{
  "name": "critical-shift-facility-greybox",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Browser-based greybox level-design prototype for the Critical Shift facility. Not production game code.",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 5173",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host 0.0.0.0 --port 4173",
    "typecheck": "tsc --noEmit",
    "validate": "vite build --config vite.validate.config.ts --logLevel warn && node dist-tools/validate.mjs",
    "check": "npm run typecheck && npm run validate && npm run build"
  },
  "dependencies": {
    "three": "^0.185.1"
  },
  "devDependencies": {
    "@types/three": "^0.185.3",
    "typescript": "^7.0.2",
    "vite": "^8.2.0"
  }
}
