# Grok Shift Prototype

Imported from the Grok app-builder export as a standalone Three.js/Vite prototype. Generated caches, auth scaffolding, PWA boilerplate, Unity `Library/`, and unreferenced screenshot dumps were intentionally omitted.

```bash
npm install
npm run dev
```
